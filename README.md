# criptolab5
Me faltan algunos puntos, pero ya no alcanzo, asi que se manda asi nomas.


EJECUCION:
En el directorio de codigo, ejecutar un "docker compose up --build" y eso va a descargar todas las imagenes, referentes a los dockerfiles de cada Cliente/Servidor y los ejecutará.

Luego puedes seguir las instrucciones del informe, para llegar a los mismos resultados que estan documentados.

Para utilizar el archivo python, para obtener los HASSH, necesitas tener instalado scapy y pyshark, aunque no pude hacer funcionar el codigo, asi que es irrelevante.
